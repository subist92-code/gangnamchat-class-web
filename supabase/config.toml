# 클래스 전용 Supabase 프로젝트(C-071 ② · 본체와 비공유).
# project_id 는 발주자가 만든 프로젝트 ref 로 바꾼다. 키는 여기 적지 않는다(H-6).
project_id = "gangnamchat-class"

[functions.passthrough-smoke]
# 로그인한 선생만 부를 수 있다(스모크 4 통과 기준).
verify_jwt = true
