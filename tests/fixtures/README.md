# 테스트 픽스처

- `class-folder/` — 규격 v0.3 대로 손으로 만든 최소 클래스 폴더(픽스처 노드 ID 만).
  lint 가 0건이어야 한다.
- `class-folder-poisoned/` — **L-11 양성 대조 픽스처**(C-046 의무).
  `문제함/leaked.json` 에 가짜 API 키 문자열이 심겨 있다. lint 가 반드시 error 를 내야 한다.
  실제 키가 아니며, 이 파일이 사라지면 「깨끗한 폴더 0건」 보고는 무효다.
